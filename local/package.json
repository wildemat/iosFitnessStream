{
  "name": "fitnessstream-local-receiver",
  "version": "1.0.0",
  "description": "Local HTTP server that receives and logs FitnessStream workout metrics",
  "main": "server.js",
  "scripts": {
    "start": "node server.js & sleep 1 && node emitter.js"
  },
  "engines": {
    "node": ">=18"
  }
}
